# CURSOR DEV PROMPT: Add Persistent Navigation Without Breaking Anything

## 🎯 OBJECTIVE
Add persistent header navigation and footer to ALL pages in the React app. Currently they only appear on the main page, trapping users in modules with no way to navigate back.

## ⚠️ CRITICAL SAFETY RULES - DO NOT BREAK THESE:

1. **DO NOT modify any existing module components** (HousingSearch.jsx, Benefits.jsx, Legal.jsx, etc.)
2. **DO NOT change any existing routing logic** in individual components
3. **DO NOT alter any existing functionality** - only ADD navigation wrapper
4. **DO NOT touch database connections or API calls**
5. **DO NOT modify any existing CSS classes or styling** within components

## 🔧 EXACT IMPLEMENTATION REQUIRED:

### Step 1: Create Layout Component
Create a new file `src/components/Layout.jsx`:

```jsx
import React from 'react';
import { Outlet } from 'react-router-dom';

const Layout = ({ children }) => {
  return (
    <div className="min-h-screen flex flex-col">
      {/* HEADER - Copy from main page */}
      <header className="bg-blue-600 text-white p-4">
        <nav className="container mx-auto">
          {/* Add navigation menu here - same as main page */}
        </nav>
      </header>

      {/* MAIN CONTENT AREA */}
      <main className="flex-1">
        {children || <Outlet />}
      </main>

      {/* FOOTER - Copy from main page */}
      <footer className="bg-gray-800 text-white p-4">
        {/* Add footer content here - same as main page */}
      </footer>
    </div>
  );
};

export default Layout;
```

### Step 2: Update App.jsx ONLY
In `App.jsx`, wrap ALL routes with the Layout component:

**BEFORE:**
```jsx
<BrowserRouter>
  <Routes>
    <Route path="/" element={<Dashboard />} />
    <Route path="/housing" element={<HousingSearch />} />
    <Route path="/benefits" element={<Benefits />} />
    {/* other routes */}
  </Routes>
</BrowserRouter>
```

**AFTER:**
```jsx
<BrowserRouter>
  <Layout>
    <Routes>
      <Route path="/" element={<Dashboard />} />
      <Route path="/housing" element={<HousingSearch />} />
      <Route path="/benefits" element={<Benefits />} />
      {/* other routes */}
    </Routes>
  </Layout>
</BrowserRouter>
```

### Step 3: Navigation Menu Content
The header navigation should include these links:

```jsx
<nav className="flex space-x-6">
  <Link to="/" className="hover:text-blue-200">Dashboard</Link>
  <Link to="/case-management" className="hover:text-blue-200">Case Management</Link>
  <Link to="/housing" className="hover:text-blue-200">Housing</Link>
  <Link to="/benefits" className="hover:text-blue-200">Benefits</Link>
  <Link to="/legal" className="hover:text-blue-200">Legal</Link>
  <Link to="/resume" className="hover:text-blue-200">Resume</Link>
  <Link to="/jobs" className="hover:text-blue-200">Jobs</Link>
  <Link to="/services" className="hover:text-blue-200">Services</Link>
  <Link to="/ai-chat" className="hover:text-blue-200">AI Assistant</Link>
  <Link to="/smart-daily" className="hover:text-blue-200">Smart Daily</Link>
</nav>
```

## 🛡️ WHAT NOT TO DO:

❌ **DO NOT** remove header/footer from main page and move to modules  
❌ **DO NOT** modify any `.jsx` files except App.jsx and new Layout.jsx  
❌ **DO NOT** change any existing component imports or exports  
❌ **DO NOT** alter any existing database calls or API endpoints  
❌ **DO NOT** modify any existing state management or hooks  
❌ **DO NOT** change any existing Tailwind classes within components  

## ✅ WHAT TO DO:

✅ **ONLY** create new Layout.jsx component  
✅ **ONLY** modify App.jsx to wrap routes with Layout  
✅ **COPY** existing header/footer styling from main page  
✅ **PRESERVE** all existing component functionality  
✅ **TEST** that all existing features still work after changes  

## 🧪 TESTING CHECKLIST:

After implementation, verify:
- [ ] All modules still load and function normally
- [ ] Navigation appears on every page
- [ ] Users can navigate between modules
- [ ] All existing buttons and forms still work
- [ ] Database operations still function
- [ ] No console errors appear
- [ ] Mobile responsiveness maintained

## 📁 FILES TO MODIFY:

1. **CREATE**: `src/components/Layout.jsx` (new file)
2. **MODIFY**: `src/App.jsx` (add Layout wrapper only)
3. **DO NOT TOUCH**: Any other existing files

## 🎯 SUCCESS CRITERIA:

When complete:
- Header navigation visible on ALL pages
- Footer visible on ALL pages  
- Users can navigate between modules from any page
- All existing functionality preserved
- No broken links or errors
- Platform feels like unified application instead of separate pages

**REMEMBER: This is ONLY a layout/navigation change. Do not modify any business logic, database operations, or existing component functionality.**